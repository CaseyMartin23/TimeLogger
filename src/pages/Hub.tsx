import React from "react";
import { Grid, Button } from "semantic-ui-react";

export const Hub = () => {
  return (
    <div className="companylists">
      <Grid columns={2}>
        <Grid.Column width={13}>awvfadsv</Grid.Column>
        <Grid.Column width={3}>
          <Button>Create a new company</Button>
        </Grid.Column>
      </Grid>
    </div>
  );
};
